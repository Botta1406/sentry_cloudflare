# Public API Configuration (SAFE to commit)
NEXT_PUBLIC_API_URL=http://localhost:8787

# ⚠️  IMPORTANT: Sensitive tokens are stored in Cloudflare Secrets Store
# Do NOT add secrets to this file or .env.local
#
# Secrets to configure in Cloudflare:
# https://dash.cloudflare.com/secrets-store
#
# Commands to add secrets:
# $ wrangler secret put SENTRY_DSN
# $ wrangler secret put SENTRY_AUTH_TOKEN  (optional)
#
# For local development, create .dev.vars file instead:
# SENTRY_DSN=your_sentry_dsn_here
